name: DevSecOps & Security Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  security-and-build-check:
    name: Security Audit & Build Verification
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install Workspace Dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Dependency Security Audit (Backend)
        run: pnpm --filter cardiocenter-backend run audit:deps
        continue-on-error: true

      - name: Generate Prisma Client
        run: pnpm exec prisma generate --schema=backend/prisma/schema.prisma

      - name: Typecheck Backend
        run: pnpm --filter cardiocenter-backend run build

      - name: Typecheck & Build Frontend
        run: pnpm --filter cardiocenter-frontend run build
